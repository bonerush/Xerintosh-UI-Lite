- 功能一:基本设置
    - 调查开发板是否支持wifi和蓝牙的连接,如果支持则加入wifi和蓝牙的链接逻辑:
        - 首先打开对应的设置选项,首先会有一个switch来让用户选择是否开启wifi/蓝牙
            - 如果开启:会开始扫描附近的wifi和蓝牙设备,扫描完毕之后会生成一个选项菜单并且弹窗提示"输入密码时需要接入串口使用",用户可以选择对应的wifi名字(蓝牙名字)点击.
            - 当点击未配置的选项的时候串口会弹出交互程序引导用户输入密码(配对码)进行连接,并且如果用户输入错误会有对应的重输入机制.
            - 当点击配置完毕的选项的时候,用户会进入下一级菜单,可选择重新连接和删除配置两个选项.
                - 重新连接即再次尝试连接
                - 删除配置则删除此配置,并且标记为未配置选项,用户退出当前菜单回到选择wifi/蓝牙菜单的时候再次点击该选项会跳到用户选择未配置选项的逻辑
            - 当用户设置完毕并且成功的时候请保存对应的设置内容,用户下一次开机的时候就可以不用再次串口连接,自动连接
    - 加入对亮度的调节,使用slider_item来进行屏幕亮度的调节.

- 功能二:加入串口调试助手
    - 首先该功能的入口就放在root菜单,用户点击进去会进入串口调试程序:
        - 顶部信息栏会显示:
            - 是否链接上主机(Connected/Disconnected)并且会有弹窗弹出告知用户是否已链接,如果没有链接则告知错误原因.
            - 波特率(Rate:115200)
            - 模式(Debug/Norm,文字请用圆角矩形作为文字背景,大小要求能合理适配顶部信息栏,颜色也要求和信息栏的颜色对比比较大)
                - Debug模式是如果当前板子处于debug模式,则会返回对应的系统debug信息,并且信息也会输入到信息栏下面的具体终端显示框
                - Norm模式就是正常的串口通信模式,电脑可以输入信息给开发板,开发板也可以对应返回信息
        - 终端栏:终端栏是一个紧贴信息栏和屏幕边缘的加粗方框,方框的右侧会有对应的滑动条,除开这些栏里面采取一种类似终端的显示,具体格式如下
            - [Master]:INFO1INFO1INFO1
            - [Slave]:info2info2info2
            - [Debug]:debugdebugdebug
        - 终端栏的信息在超出终端栏会自动下滑信息,来保证终端栏的正常显示
    - 用户长按退出的时候会显示还需摁多长时间才能退出